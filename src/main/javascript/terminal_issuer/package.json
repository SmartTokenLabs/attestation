{
  "name": "issuer",
  "version": "1.0.0",
  "description": "",
  "type": "commonjs",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build src/index.ts -o dist",
    "build-prod": "ncc build src/index.ts -o dist -m",
    "watch": "ncc build src/index.ts -o dist -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tokenscript/attestation": "^0.5.1",
    "ethers": "^5.7.2"
  }
}
